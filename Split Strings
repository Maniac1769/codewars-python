def solution(s):
    list=[]
    for i in range(0,len(s),2):
        a=s[i:i+2]
        if len(a)==1:
            a=a+'_'
            list.append(a)
        else:
            list.append(a)
    return list
